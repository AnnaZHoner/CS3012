#include "pch.h"
#include "../LCA.h"



TEST(Test1, TestFullGraph)
{

	Node * root = newNode(1);
	root->left = newNode(2);
	/*root->right = newNode(3);
	root->left->left = newNode(4);
	root->left->right = newNode(5);
	root->right->left = newNode(6);
	root->right->right = newNode(7);


	EXPECT_EQ(findLCA(root, 4, 5), 2);
	//EXPECT_TRUE(true);

	*/

}
